name: Real Case D Production Scale Validation
on:
  push:
    branches: ['experiment/borderless-scale-hierarchy']
    paths:
      - 'benchmark/real-case-d/**'
      - 'measurement-service/**'
      - '.github/workflows/real-case-d-production.yml'
permissions:
  contents: read
jobs:
  case-d:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      RULERNET_ALLOW_NONCOMMERCIAL_DOWNLOAD: 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r measurement-service/requirements.txt pytest
      - name: Measurement tests
        run: pytest -q measurement-service/tests
      - name: Warm RulerNet
        run: PYTHONPATH=measurement-service python -c "from rulernet import _load_session; _load_session(); print('RulerNet ready')"
      - name: Run Case D through production scale inference
        run: |
          mkdir -p acceptance-results
          python benchmark/real-case-d/run.py | tee acceptance-results/run.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: real-case-d-production
          path: acceptance-results/
          retention-days: 30
